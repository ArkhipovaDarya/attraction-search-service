spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/attraction
    username: postgres
    password: 901298
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
     enabled: true
     schemas: public

jwtTimeOfLifeMs: 3600000